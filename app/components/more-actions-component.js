import Component from '@ember/component';

export default Component.extend({
    label: 'Еще 154992632640 активностей',
    actions: {
        onClickHandler() {
            alert('Out of memory exception')
        }
    }
});
